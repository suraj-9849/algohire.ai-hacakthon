version: '3.8'

services:
  # Frontend - Next.js App with Firebase
  frontend:
    build: ./apps/web
    ports:
      - "3000:3000"
    environment:
      # Firebase configuration will be handled via .env files
      - NODE_ENV=production